#include "main.h"
#include <string.h>
#include <ctype.h>
#include <stdlib.h>
#include <stdio.h>

/**
 * _memset - sets strings to a certain lenth.
 *@s: the string
 *@b: string 2
 *@n: the number
 * 
 * Return: the string.
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return memcpy(dest, src, n);
}
