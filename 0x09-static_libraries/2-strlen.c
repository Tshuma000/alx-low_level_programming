#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - calculates the length of a string.
 *@s: the string
 * 
 * Return: The length
 */

int _strlen(char *s)
{
	return strlen(s);
}
