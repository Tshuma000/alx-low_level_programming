#include "main.h"

/**
 * _islower - checks if character is lowercase
 *@c: character to be checked
 * Return: On success 1, on failure 0.
 */

int _islower(int c)
{
	if (c > 91)
	{
		return (1);
	}

	return (0);
}
